/**
 * @deprecated This file is being phased out in favor of the modular structure in ./api/
 * All exports are re-exported from the new modular structure for backward compatibility.
 * New code should import directly from @/services/api which uses the new structure.
 */

// Re-export everything from the new modular structure
export * from "./api/index";
